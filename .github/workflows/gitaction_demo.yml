# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: Java CI with gitaction

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  terraform:
    name: 'Terraform'
    runs-on: ubuntu-latest

    # Use the Bash shell regardless whether the GitHub Actions runner is ubuntu-latest, macos-latest, or windows-latest
    # Set the working directory to main for the config files
    defaults:
      run:
        shell: bash
        working-directory: ${{ github.workspace }}/target
  
  #build maven package and save to target file
  build-maven-docker:

    runs-on: ubuntu-latest
      
    steps:
    - uses: actions/checkout@v3
    - name: Set up JDK 11
      uses: actions/setup-java@v3
      with:
        java-version: '11'
        distribution: 'temurin'
        cache: maven
    - name: Cache local Maven repository
      uses: actions/cache@v2
      with:
        path: ~/.m2
        key: ${{ runner.os }}-maven-${{ hashFiles(./aksdemo/pom.xml') }}
        restore-keys: |
                      ${{ runner.os }}-maven-     
    - name: Build with Maven
      run: |
        mvn -B package --file ./aksdemo/pom.xml
        run: docker build . --file ./aksdemo/Dockerfile --tag aksdemo:$(date +%s)
        
       
          
    

        run: docker build . --file ./aksdemo/Dockerfile --tag aksdemo:$(date +%s)
